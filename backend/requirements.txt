# Core API (required on Render)
Flask
python-dotenv
firebase-admin
Flask-Cors
gunicorn

# OCR / NER (optional locally; heavy on free Render)
# pytesseract
# spacy
# opencv-python
# pdf2image
# google-cloud-vision
